import { SlideEntranceDirective } from './onVisibleAnimation.directive';

describe('SlideEntranceDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideEntranceDirective();
    expect(directive).toBeTruthy();
  });
});
